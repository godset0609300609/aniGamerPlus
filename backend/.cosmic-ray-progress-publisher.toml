[cosmic-ray]
module-path = "app/services/telegram_progress_publisher.py"
timeout = 60.0
excluded-modules = []
test-command = "uv run pytest tests/test_telegram_progress_publisher.py -W error --no-cov -p no:cacheprovider -x"

[cosmic-ray.distributor]
name = "local"
